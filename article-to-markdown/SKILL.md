---
name: article-to-markdown
description: 将网页文章保存为 Markdown 文件 — 获取 URL 内容、添加 YAML Front Matter、保存到当前目录
metadata:
  type: skill
  trigger:
    - "保存文章"
    - "article to markdown"
    - "网页转markdown"
    - "保存网页"
    - "下载文章"
    - "/article-to-markdown"
  os: [windows, linux, macos]
  shell: [powershell, bash]
---

# Article to Markdown — 执行规范

## 核心原则

1. **原样保存**: 保留原文内容，不做修改或润色
2. **原图链接**: 图片使用原始 URL，不下载不转存
3. **YAML Front Matter**: 文件第一行插入元数据头，category 和 tag 根据文章内容自动生成
4. **本地存储**: 保存到用户指定目录，未指定则保存到当前工作目录
5. **标题不入正文**: 文章标题仅用于生成文件名，不出现在 Markdown 正文中
6. **标题层级修正**: 文章无一级标题时，所有标题自动提升一级

---

## 执行流程

### 阶段 1: 获取参数

向用户确认以下信息（用户未指定则由 AI 根据文章内容自动生成）：

| 参数 | 生成方式 | 说明 |
|------|----------|------|
| `category` | AI 根据文章内容推断 | 文章分类，最多 2 个词语 |
| `tag` | AI 根据文章内容提取 | 内容标签，最多 2 个词语 |
| `status` | `publish` | 发布状态 |
| `filename` | 自动从标题提取 | 保存的文件名 |

用户可在提供 URL 时一并指定：
- `保存文章 https://xxx.com/xxx --category 技术 --tag python,AI --filename my-note`

### 阶段 2: 获取网页内容

1. 使用 `WebFetch` 工具获取 URL 对应网页的完整内容
2. WebFetch 会自动将 HTML 转换为 Markdown
3. 如果网页需要认证或获取失败，提示用户并退出

### 阶段 3: 内容处理

对获取到的内容做以下处理：

1. **提取标题**: 取第一个 `#` 或 `<h1>` 标题作为文章标题，用于生成文件名
2. **标题不入正文**: 删除正文中的文章标题行（`# 标题`），不保留在 Markdown 文件中
3. **移除来源信息**: 删除 "文章来源"、"原文链接"、"来源"、"转载自" 等行
4. **移除更新时间**: 删除 "最后更新"、"更新时间"、"发布于"、"Posted on"、"本文最后更新于" 等行
5. **移除页脚推广**: 删除文末的 "小提示"、"订阅"、"关注" 等网站推广内容
6. **保留原图链接**: 不做任何修改，保持 `![](url)` 原样
7. **自动生成标签**: 根据文章内容推断 category 和 tag（各不超过 2 个词语）
8. **标题提升**: 如果文章中没有一级标题（`# `），将所有标题提高一级（`## ` → `# `，`### ` → `## `，以此类推）
9. **保留原文内容**: 除上述移除项外，其余内容原样保留

### 阶段 4: 生成文件名

- 从文章标题提取，去除非法字符（`<>:"/\|?*`）
- 中英文混排时保留原样
- 长度控制在 50 字符以内
- 扩展名 `.md`
- 保存到用户指定目录，未指定则保存在当前工作目录

### 阶段 5: 写入文件

```markdown
---
category: [<用户指定的 category>]
tag: [<用户指定的 tag>]
status: <用户指定的 status>
---

<处理后的内容>
```

- 使用 `Write` 工具写入文件
- 写入前检查文件是否已存在，若存在则终止任务并提示"文件已存在: <路径>"

---

## 参数速查

| 参数 | 效果 |
|------|------|
| `--category <名称>` | 指定文章分类（最多 2 个词语），未指定则由 AI 自动生成 |
| `--tag <标签>` | 指定标签（逗号分隔，最多 2 个），未指定则由 AI 自动生成 |
| `--status <状态>` | 指定发布状态，默认 `publish` |
| `--filename <名称>` | 指定保存文件名，默认从标题提取 |
| `--path <目录>` | 指定保存目录，默认当前工作目录 |

---

## 输出格式

```
✅ 文章已保存
📝 标题: <文章标题>
📁 文件: <文件名>.md
📂 路径: <保存目录>/<文件名>.md
🏷️  分类: <category>
📌 标签: <tag>
```

---

## 错误处理

| 场景 | 处理方式 |
|------|----------|
| URL 无法访问 | 提示用户检查链接是否有效 |
| 网页需要登录 | 提示用户提供可直接访问的 URL |
| 内容为空 | 提示"未能提取到文章内容"并退出 |
| 文件已存在 | 终止任务，提示"文件已存在: <路径>" |
| 标题无法提取 | 使用 URL 的最后一段作为文件名 |

---

## 重要约束

1. **不修改原文**: 只移除来源/更新时间/页脚推广/标题，不对正文做任何编辑
2. **不下载图片**: 所有图片保持原始链接
3. **不显示来源**: 最终文件中不含文章来源和更新时间
4. **不做 AI 摘要**: 只保存原文，不添加任何 AI 生成的内容
5. **标题不入正文**: 文章标题仅用于 frontmatter 和文件名，不出现在正文中
6. **标签自动生成**: category 和 tag 根据文章内容推断，各不超过 2 个词语
7. **标题层级修正**: 无一级标题时自动提升所有标题层级
8. **编码为 UTF-8**: 中文内容正确保存
